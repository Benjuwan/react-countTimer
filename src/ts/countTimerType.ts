export type countTimerType = {
    year: string;
    month: string;
    dayDate: string;
    hour: string;
    minute: string;
    second?: string;
}

export type monthsAndDaysType = {
    months: number;
    days: number;
}